name: example-inference-orchestrator
channels:
  - defaults
dependencies:
    - python=3.10
    - pip=23.3
    - pip:
      - black
      - coverage
      - flake8
      - isort
      - pytest
      - pre-commit
      - sphinx
      - python-magic
      - pylint
      - -r requirements.txt
